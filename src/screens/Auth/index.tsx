import React from 'react';
import {Text, View} from 'react-native';
import WText from '../../components/WText';
import Colors from '../../themes/Colors';

const AuthScreen = () => {
  return (
    <View>
      <WText color={Colors.black}>AuthScreen1111111</WText>
    </View>
  );
};

export default AuthScreen;
